version: "2"
sql:
- engine: "postgresql"
  queries: "sql/queries.sql"
  schema: "sql/schema.sql"
  gen:
    go:
      package: "db"
      out: "internal/db"
      overrides:
        - db_type: pg_catalog.varchar
          go_type: string
          nullable: true
